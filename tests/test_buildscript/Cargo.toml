[package]
name = "test_buildscript"
version = "0.1.0"
edition = "2024"

[dependencies]
license-fetcher = { path = "../.." }

[build-dependencies]
license-fetcher = { path = "../..", features = ["build"] }
